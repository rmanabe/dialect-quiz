// AdMob's native SDK has no web target. Web builds simply show no banner here;
// if web monetization is wanted later, wire up Google AdSense/Ad Manager for web separately.
export default function AdBanner() {
  return null;
}
